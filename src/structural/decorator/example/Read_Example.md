## 인코딩 및 압축 데코레이터들
- 이 예시는 코드를 변경하지 않고 객체의 행동을 조정하는 방법을 보여줌
- 처음에 비즈니스 로직 클래스는 일반 텍스트로만 데이터를 읽고 쓸 수 있었으나, 그 다음 표준 작업을 래핑 된 객체 내에 실행 후 새로운 행동들을 추가하는 몇 개의 작은 래퍼 클래스들 생성
- 첫 번째 래퍼는 데이터를 암호화 및 해독, 두 번째 래퍼는 데이터를 압축 및 추출
- 또 한 데코레이터를 다른 데코레이터로 래핑하는 식으로 래퍼를 결합할 수도 있음